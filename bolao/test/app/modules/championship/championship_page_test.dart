import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:bolao/app/modules/championship/championship_page.dart';

main() {
  testWidgets('ChampionshipPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ChampionshipPage(title: 'Championship')));
    //  final titleFinder = find.text('Championship');
    //  expect(titleFinder, findsOneWidget);
  });
}
