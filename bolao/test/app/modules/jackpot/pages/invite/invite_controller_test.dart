import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:bolao/app/modules/jackpot/pages/invite/invite_controller.dart';
import 'package:bolao/app/modules/jackpot/jackpot_module.dart';

void main() {
  initModule(JackpotModule());
  // InviteController invite;
  //
  setUp(() {
    //     invite = JackpotModule.to.get<InviteController>();
  });

  group('InviteController Test', () {
    //   test("First Test", () {
    //     expect(invite, isInstanceOf<InviteController>());
    //   });

    //   test("Set Value", () {
    //     expect(invite.value, equals(0));
    //     invite.increment();
    //     expect(invite.value, equals(1));
    //   });
  });
}
