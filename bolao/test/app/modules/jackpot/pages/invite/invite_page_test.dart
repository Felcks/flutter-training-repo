import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:bolao/app/modules/jackpot/pages/invite/invite_page.dart';

main() {
  testWidgets('InvitePage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(InvitePage(title: 'Invite')));
    //  final titleFinder = find.text('Invite');
    //  expect(titleFinder, findsOneWidget);
  });
}
